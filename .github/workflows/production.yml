name: Deploy Application

on:
  push:
    branches: ["staging"]  # Trigger deploy ke production hanya setelah staging

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v3

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v2

      - name: Login to Docker Hub
        uses: docker/login-action@v2
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Build and push app image
        uses: docker/build-push-action@v6
        with:
          context: .
          file: ./Docker/dockerfile/app.Dockerfile
          push: true
          tags: johanesrendy/laravel:${{ github.run_number }}

      - name: Build and push nginx image
        uses: docker/build-push-action@v6
        with:
          context: .
          file: ./Docker/dockerfile/nginx.Dockerfile
          push: true
          tags: johanesrendy/laravel-nginx:${{ github.run_number }}

      - name: Build and push npm image
        uses: docker/build-push-action@v6
        with:
          context: .
          file: ./Docker/dockerfile/npm.Dockerfile
          push: true
          tags: johanesrendy/laravel-npm:${{ github.run_number }}

  deployment:
    needs: build
    runs-on: ubuntu-latest
    steps:
      - name: Deploy to VPS
        uses: appleboy/ssh-action@master
        with:
          host: "3.106.237.6"
          username: ${{ secrets.SERVER_USERNAME }}
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          port: ${{ secrets.SERVER_PORT }}
          script: |
            cd ~/bigdashboard_pso
            git checkout staging  # Pastikan kita menarik kode dari branch staging, bukan dev

            # Update IMAGE_VER in .env
            sed -i '$d' .env && echo "IMAGE_VER=${{ github.run_number }}" >> .env

            # Restart Nginx
            sudo nginx -t
            sudo service nginx restart
            sudo systemctl status nginx

            git pull origin staging  # Pastikan kode yang di-pull adalah dari branch staging

            # Install Composer dependencies
            composer i

            # Docker: Bring down the current containers and rebuild them
            sudo docker-compose --env-file .env down
            sudo docker compose --env-file .env build 

            # Remove public hot
            sudo rm -r public/hot

            sudo docker compose --env-file .env up -d 

            # Show logs for debugging
            echo "log laravel"
            sudo docker logs laravel
            echo "log nginx"
            sudo docker logs nginx
            echo "log npm"
            sudo docker logs npm
